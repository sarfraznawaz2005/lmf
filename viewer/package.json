{
	"name": "lmf-viewer",
	"version": "1.0.0",
	"description": "Desktop viewer for LMF (LLM Markup Format) files",
	"scripts": {
		"start": "electrobun dev",
		"dev": "electrobun dev --watch",
		"build": "electrobun build --env=stable"
	},
	"dependencies": {
		"electrobun": "1.15.1"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
